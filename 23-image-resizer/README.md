# 23 — Batch Image Resizer

Resizes one image or every supported image in a folder while preserving aspect ratio and avoiding accidental enlargement by default.

```bash
python main.py photos resized --width 1200 --height 1200
python test_main.py
```

**Skills:** Pillow, batch processing, aspect-ratio math, pathlib.
