# 14 — Task Manager CLI

A persistent command-line task manager with add, list, complete, and delete commands. Tasks use stable numeric IDs.

```bash
python main.py --file tasks.json add "Finish Python project"
python main.py --file tasks.json list
python test_main.py
```

**Skills:** JSON storage, CRUD, stable identifiers, CLI subcommands.
