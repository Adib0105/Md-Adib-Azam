# Library Management System

Manages borrow and return events and identifies overdue library loans.

## Run

    python app.py
    python -m unittest test_app.py

Use --input your-file.json for your own data. The included sample is synthetic.
