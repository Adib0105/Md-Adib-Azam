# Web Log Analyzer

Parses common web access logs and reports status codes, top IPs and error paths.

## Run

    python app.py
    python -m unittest test_app.py

Use --input your-file.json for your own data. The included sample is synthetic.
