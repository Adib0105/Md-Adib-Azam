# URL Health Monitor

Validates URLs and summarizes HTTP probe results supplied by an authorized monitor.

## Run

    python app.py
    python -m unittest test_app.py

Use --input your-file.json for your own data. The included sample is synthetic.
