# Automation Report Generator

Converts automation run logs into an HTML report with success rate and failures.

## Run

    python app.py
    python -m unittest test_app.py

Use --input your-file.json for your own data. The included sample is synthetic.
