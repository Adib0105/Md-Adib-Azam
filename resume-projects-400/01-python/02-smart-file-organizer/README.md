# Smart File Organizer

Creates a collision-safe dry-run plan that groups filenames by extension.

## Run

    python app.py
    python -m unittest test_app.py

Use --input your-file.json for your own data. The included sample is synthetic.
