# Number Base Converter

Converts a decimal number into binary, octal and hexadecimal forms.

## Build and run

    make
    ./app

The program uses embedded synthetic demo data for a deterministic smoke test.
