# 15 — Safe File Organizer

Organizes files into category folders based on extension. It runs in preview mode by default and moves files only when `--apply` is supplied.

```bash
python main.py ~/Downloads
python main.py ~/Downloads --apply
python test_main.py
```

**Skills:** pathlib, safe automation, dry-run design, file operations.
