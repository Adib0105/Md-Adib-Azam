# 19 — Unit Converter

A tested converter for length, weight, and temperature units with a clean extensible conversion model.

```bash
python main.py 10 km mi
python main.py 32 c f
python test_main.py
```

**Skills:** pure functions, validation, conversion formulas, CLI interfaces.
