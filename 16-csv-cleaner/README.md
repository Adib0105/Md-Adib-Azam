# 16 — CSV Cleaner

Cleans whitespace, normalizes empty cells, and removes duplicate rows from CSV files while preserving column order.

```bash
python main.py input.csv cleaned.csv
python test_main.py
```

**Skills:** CSV processing, data quality, deduplication, CLI design.
