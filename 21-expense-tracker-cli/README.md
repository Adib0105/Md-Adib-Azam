# 21 — Expense Tracker CLI

A JSON-backed expense tracker that records dated transactions and reports totals by category.

```bash
python main.py --file expenses.json add 450 Food --note "Groceries"
python main.py --file expenses.json report
python test_main.py
```

**Skills:** JSON persistence, Decimal arithmetic, reporting, CLI subcommands.
