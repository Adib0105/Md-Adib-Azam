# 13 — Terminal Quiz App

A reusable multiple-choice quiz engine with answer normalization, scoring, and a built-in Python basics quiz.

```bash
python main.py
python test_main.py
```

**Skills:** functions, loops, input validation, reusable program design.
