# 11 — Password Strength Checker

A privacy-friendly CLI that scores password structure locally and returns clear improvement suggestions. It never saves or transmits the password.

```bash
python main.py "ExamplePassword123!"
python test_main.py
```

**Skills:** regular expressions, defensive validation, CLI design, unit testing.
