# 24 — File Checksum Generator

Calculates SHA-256 checksums for one file or creates a sorted checksum manifest for an entire folder.

```bash
python main.py document.pdf
python main.py project-folder --manifest checksums.txt
python test_main.py
```

**Skills:** hashlib, binary streaming, integrity verification, recursive file handling.
